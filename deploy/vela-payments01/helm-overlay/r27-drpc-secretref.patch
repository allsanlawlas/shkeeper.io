--- a/templates/deployments/ethereum-shkeeper.yaml
+++ b/templates/deployments/ethereum-shkeeper.yaml
@@ -64,7 +64,10 @@
             value: &CURRENT_ETH_NETWORK_VALUE "sepolia"
           {{- end }}
           - name: FULLNODE_URL
-            value: {{ .Values.eth_fullnode.url }}
+            valueFrom:
+              secretKeyRef:
+                name: shkeeper-drpc-ethereum
+                key: endpoint
           {{- if .Values.ethereum_shkeeper.external_drain_config }}
           - name: EXTERNAL_DRAIN_CONFIG
             value: {{ .Values.ethereum_shkeeper.external_drain_config | toJson | quote }}
@@ -102,7 +105,10 @@
             value: *CURRENT_ETH_NETWORK_VALUE
           {{- end }}
           - name: FULLNODE_URL
-            value: {{ .Values.eth_fullnode.url }}
+            valueFrom:
+              secretKeyRef:
+                name: shkeeper-drpc-ethereum
+                key: endpoint
           {{- if .Values.ethereum_shkeeper.external_drain_config }}
           - name: EXTERNAL_DRAIN_CONFIG
             value: {{ .Values.ethereum_shkeeper.external_drain_config | toJson | quote }}
--- a/templates/deployments/polygon-shkeeper.yaml
+++ b/templates/deployments/polygon-shkeeper.yaml
@@ -64,7 +64,10 @@
             value: &CURRENT_POLYGON_NETWORK_VALUE "amoy"
           {{- end }}
           - name: FULLNODE_URL
-            value: {{ .Values.polygon_fullnode.url }}
+            valueFrom:
+              secretKeyRef:
+                name: shkeeper-drpc-polygon
+                key: endpoint
           {{- range $name, $value := .Values.polygon_shkeeper.extraEnv }}
           - name: {{ $name | quote }}
             value: {{ $value | quote }}
@@ -98,7 +101,10 @@
             value: *CURRENT_POLYGON_NETWORK_VALUE
           {{- end }}
           - name: FULLNODE_URL
-            value: {{ .Values.polygon_fullnode.url }}
+            valueFrom:
+              secretKeyRef:
+                name: shkeeper-drpc-polygon
+                key: endpoint
           - name: C_FORCE_ROOT
             value: "1"
           {{- range $name, $value := .Values.polygon_shkeeper.extraEnv }}
--- a/templates/deployments/solana-shkeeper.yaml
+++ b/templates/deployments/solana-shkeeper.yaml
@@ -64,7 +64,10 @@
             value: &CURRENT_SOL_NETWORK_VALUE "devnet"
           {{- end }}
           - name: FULLNODE_URL
-            value: {{ .Values.solana_fullnode.url }}
+            valueFrom:
+              secretKeyRef:
+                name: shkeeper-drpc-solana
+                key: endpoint
           {{- if .Values.solana_shkeeper.external_drain_config }}
           - name: EXTERNAL_DRAIN_CONFIG
             value: {{ .Values.solana_shkeeper.external_drain_config | toJson | quote }}
@@ -100,7 +103,10 @@
             value: *CURRENT_SOL_NETWORK_VALUE
           {{- end }}
           - name: FULLNODE_URL
-            value: {{ .Values.solana_fullnode.url }}
+            valueFrom:
+              secretKeyRef:
+                name: shkeeper-drpc-solana
+                key: endpoint
           {{- if .Values.solana_shkeeper.external_drain_config }}
           - name: EXTERNAL_DRAIN_CONFIG
             value: {{ .Values.solana_shkeeper.external_drain_config | toJson | quote }}
